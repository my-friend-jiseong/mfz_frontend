# 데모 녹화 · 스크린샷 재캡처 가이드 (기기 캡처)

> 목적: **기기에서 데모 동선을 한 번 제대로** 진행하면서 → ① 데모 영상 ② 남은 정지 스크린샷(구버전 4장 교체 + 빠른 촬영 신규)을 동시에 확보한다.
> 왜 기기인가: 웹/헤드리스 캡처는 테스트 데이터(글자깨짐·방문 0건·사진 없음)라 hero 컷이 안 나온다. 실데이터·실사진·실GPS는 기기에서만.
> 산출물 저장 위치: `docs/presentation/screenshots/` — 기존 번호 체계를 따른다 (교체분은 같은 번호 덮어쓰기, 신규는 `23_quick_photo.png` 식으로 이어서). 인벤토리 기준은 `MVP_INFORMATION.md` §7.

---

## 0. 사전 준비 — "깔끔한 데이터" 1세트 (★ 가장 중요)

지금 계정 데이터가 지저분(`체크인 테스트용 외근`, `해으으응`, `����2`, 방문 0건)하므로, **발표 전용으로 깔끔한 1세트**를 새로 만든다.

- **현장 2~3곳**: 실제 주소로 카카오 검색해 등록 — **데모 데이터 지역은 부산 실주소 기준**. **분류·상세주소에 이모지/특수문자 금지**(글자깨짐 원인). 예) "부산 연제구 중앙대로 1001", 분류 `정기점검`.
- **현장 이름·보고서·외근 제목 현실적으로**: `테스트`/`ㅁㄴㅇㄹ` 금지. 예) 외근 "연제구 정기점검 1차", 보고서 "연제구 정기점검 1차 결과".
- **완결된 외근 1건**: 현장 2곳을 실제로 체크인하고 **전·중·후 사진을 실제로 촬영/첨부**, 방문결과 상태 선택까지. → 이게 있어야 체크인·보고서 컷이 채워진다.
- **사진**: 보기 좋은 현장 느낌의 사진 2~3장(전/중/후). 인물·민감정보 없게.

## 1. 녹화·캡처 방법 (한 번에 둘 다)

- 기기 **화면 기록**(iOS 제어센터 / Android 화면 녹화) **켜고 전체 동선 1회** 진행 → **데모 영상**.
- 동시에, 아래 ★ 표시 화면에서 **잠깐 멈추고 기기 스크린샷**(전원+볼륨) → **정지컷**.
  - 정지컷은 영상 프레임 추출보다 **기기 스크린샷이 더 선명**하다. 영상에서 뽑아야 하면 `ffmpeg -ss <시각> -i demo.mp4 -frames:v 1 out.png`.
- **품질 체크**: 세로 모드 · 라이트 모드 · 상단 상태바 깨끗하게(알림/배터리 경고 제거) · 각 ★ 화면 2~3초 정지.

## 2. 동선 + 캡처 매핑 (★ = 스크린샷 지점, `MVP_INFORMATION.md` §8 동선과 동일)

| 순서 | 동작 | 스크린샷 | 산출 파일 |
|---|---|---|---|
| 1 | 로그인 → 외근 탭 | | (영상 시작) |
| 2 | 현장 등록(단일 화면) — 주소검색 → 핀 지도 좌표 확인 | ★ 검색 결과 / ★ 장소명 검색 / ★ 핀 지도 | `12`·`13`·`14` **교체** (구버전 단계식 UI) |
| 3 | (아무 현장) 현장 수정 진입 | ★ 단일 화면 수정 | `11` **교체** (구버전 UI) |
| 4 | 외근 시작 — 현장 2곳 선택 → 순서 자동정렬 → 시작 | (기존 컷 유효) | — |
| 5 | active 외근 — 목적지 도착 표시 + 길찾기 | (기존 컷 유효) | — |
| 6 | 체크인 — 전·중·후 사진 + 방문결과 선택 | (기존 컷 유효) | — |
| 7 | **빠른 촬영** — 버튼 → 최근접 현장 매칭 → 사진 등록 | ★ 매칭 화면 | `23_quick_photo` **신규** |
| 8 | 외근 종료 → 보고서 마법사 → Word 다운로드 | (기존 컷 유효) | — |
| 9 | 지도 대시보드 — 마커→히트맵→단계구분도 + 기간 필터 | (기존 컷 유효) | — |

> 기존 22장 중 ⚠️ 4장(`11`~`14`)만 교체하면 되고, 나머지는 현행 UI와 일치한다 (`MVP_INFORMATION.md` §7 인벤토리 참조).

## 3. 도식은 Claude Desktop 재량

문제 정의·라이프사이클·아키텍처·현장 재정의 같은 그림 소재는 `MVP_INFORMATION.md`(§2·§5·§6·§7)에 사실로 정리돼 있다. 어떤 걸 도식화할지·어떻게 그릴지는 Desktop이 판단한다.
