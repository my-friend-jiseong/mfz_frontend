# 안드로이드 스토어 출시 준비도 — 차단 요소 정리

> **상태**: 착수 (2026-06-07 실측 감사 → 2026-07-28 잔여 차단 해소 → **2026-07-29 차단 1건 추가 발견**)
> **결론**: 초기 4건은 모두 해소됐다. 다만 **⛔-5 계정 삭제**가 새로 드러났고, 이건 **백엔드가 없으면 못 닫는다**. 프론트는 선반영을 마쳤다.
>
> ⚠️ **2026-07-29 정정**: 이 문서는 "코드 쪽 차단은 없다 → production 빌드 검증 단계로 진행 가능" 으로 닫혀 있었다. 그 판단은 **계정 생성 앱의 계정 삭제 요건을 감사 범위에 넣지 않은 상태**에서 내려진 것이다. 초기 감사(2026-06-07)가 아이콘·URL·키·권한 4축만 봤고 계정 수명주기를 보지 않았다. 항목별 결론은 유효하나 "차단 없음" 이라는 전체 결론은 틀렸다.

## ✅ 이미 갖춰진 것 (재작업 불요)

| 항목 | 상태 |
|---|---|
| EAS 빌드 설정 | `eas.json` — preview(apk)·production(aab) 프로필, versionCode 원격 관리 |
| 타깃 SDK | Expo SDK 54 → API 36. Play 타깃 API 요건(35+) 충족 |
| 운영 백엔드 | `ilgayo.co.kr` 라이브. 파일 URL 절대화 등 프로드 이슈 정리됨 |
| 크래시 모니터링 | Sentry 연동. 프로드 번들 크래시 2건 수정 이력(2026-06-05) — 실기기 프로드 검증 존재 |
| 권한 선언 | 위치·카메라·사진 한국어 문구 정비 — ⛔-4 해소로 실용도와 일치 |

## ⛔ 차단 — 출시 전 필수 (2026-06-07 실측 확인, 2026-07-29 §5 추가)

| # | 차단 요소 | 실측 근거 | 해소 방법 | 담당 |
|---|---|---|---|---|
| 1 | ~~앱 아이콘 플레이스홀더~~ | 2026-06-07 당시 `icon.png`·`adaptive-icon.png` 둘 다 파란 바탕 "M" | **✅ 해소** — 브랜드 아이콘 반영(2026-07-24, 사용자) + **adaptive foreground 재가공**(2026-07-28, 커밋 `fb0dd79`). 기존 adaptive 는 둥근 사각형이 이미지에 구워져 런처 마스크와 이중으로 걸리고 아트가 세이프존(675px)을 19px 넘었다 — `icon.png` 배경만 걷어내 재작성, 핵심 콘텐츠 bbox `x 263..760 · y 195..804` 로 세이프존 내 확인. `splash-icon.png`·`favicon.png` 는 문제 없음 | 디자인(사용자) + 프론트 ✅ |
| 2 | ~~개인정보 처리방침 URL 사망~~ | 앱 내 링크 `ilgayo.kr/terms`·`/privacy` → 도메인 미해석(000). `ilgayo.co.kr` 쪽도 404 | **✅ 페이지 해소** — §23 배포로 `GET /privacy`·`/terms` 200. 프론트 URL 은 운영 도메인으로 선반영돼 있어 그대로 살아났다. ⚠️ **잔여(코드 아님)**: 서빙 중인 본문이 **초안** — Play Console 심사 전 법적 문구 교체 필요 | 백엔드 ✅ / 본문은 팀 |
| 3 | ~~production 빌드에 카카오 키 미주입~~ | `eas.json` 의 키가 preview 프로필에만 존재 → production aab 는 지도가 placeholder 인 채로 빌드됨 | **✅ 해소 (2026-06-07)** — production env 에 키 주입. `EXPO_PUBLIC_*` 키는 어차피 번들에 포함되는 공개 키(카카오 콘솔의 도메인/앱 제한으로 통제)라 eas.json 명시를 허용. **EAS env 이전 + 키 재발급은 권장 후속** (`eas env:create` — 계정 인증 필요해 사용자 작업) | 프론트 ✅ |
| 4 | ~~폐기 기능 권한 잔재 + 문구 불일치~~ | 음성 메모는 ERD v2 폐기인데 `RECORD_AUDIO`·`MODIFY_AUDIO_SETTINGS`·`expo-av` 플러그인·마이크 문구 잔존. 위치 문구는 "도착 자동 감지"(제거된 기능) 기준 | **✅ 해소 (2026-06-07)** — `expo-av` 의존·플러그인·iOS 마이크 문구 제거(소스 import 0건 확인), 오디오 권한은 `blockedPermissions` 로 매니페스트 머지까지 차단, 위치 문구를 실제 용도(현장 위치 등록·빠른 촬영 매칭·내 위치 표시)로 교체. `expo config --type prebuild` 반영 검증 | 프론트 ✅ |
| 5 | **앱 내 계정 삭제 경로가 없다** | 2026-07-29 운영 OpenAPI(`ilgayo.co.kr/api-docs.json`) 실측 — `/api/me` 는 `get`·`patch` 뿐, 사용자 삭제 엔드포인트가 스펙 전체에 0건. 무인증 프로브로 재확인: `GET`/`PATCH /api/me` → 401(라우트 존재), `DELETE /api/me` → 404(없는 경로 `/api/zzz-nonexistent` 와 동일) | **프론트 ✅ 선반영 / 백엔드 ⛔ 대기** — 탈퇴 화면·스토어·API 계약 구현(`c577c31`). 서버가 없는 동안은 404/405/501 을 **명시적 실패**로 처리하고 로컬 세션을 지우지 않는다(탈퇴한 줄 알고 떠나는데 계정이 남는 상태 방지). **백엔드 backlog §30-A** 로 요청 | 프론트 ✅ / **백엔드** |

> **⛔-5 를 왜 차단으로 보는가**: **출시 대상은 Play 뿐이다**(App Store 는 계획에 없음 — 2026-07-29 확인). Google Play 는 계정 생성을 허용하는 앱에 **계정·데이터 삭제 경로를 요구**하며 **데이터 안전 양식 기재 대상**이다. 지금은 운영팀 이메일 수동 처리라 **처리방침이 말하는 절차와 실제 서비스가 어긋난다**.
>
> ⚠️ **Play 는 앱 밖에서 접근 가능한 삭제 요청 URL 도 요구하는 것으로 알려져 있다.** 앱 내 경로만으로 충분한지, 별도 웹 페이지가 필요한지는 **Play Console 데이터 안전 양식에서 직접 확인**해야 한다 — 정책 판단이라 여기서 단정하지 않는다. 필요하다면 ⛔-2 의 정책 페이지 서빙에 한 건 더 붙는다(백엔드 작업 증가).

## ⚠️ 절차·품질 — 차단은 아니나 일정 요인

| 항목 | 내용 |
|---|---|
| **비공개 테스트 요건 — 개인 계정 확정** | 계정 유형이 **개인(사업자 없음)** 으로 확인됐다(2026-07-29). Play 개인 개발자 계정 신규는 **테스터 12명 × 14일 연속 비공개 테스트** 후에야 프로덕션 승격이 가능하다 → **최소 +2주 확정**. ⚠️ **14일 시계는 지금 시작할 수 없다** — 비공개 테스트 트랙도 심사·데이터 안전 양식을 거치므로 ⛔-5(계정 삭제)가 닫혀야 올릴 수 있다. 즉 **백엔드 `DELETE /api/me` 가 전체 일정의 임계 경로**다 |
| **테스터 12명 확보** | 14일 **연속** 참여가 요건이다. 중간에 이탈하면 카운트가 흔들릴 수 있으므로 여유 있게 모집할 것. 실제 사용까지 요구되는지는 Play Console 문구로 확인 |
| **개인 계정 — 사업자 부재의 파급** | ① 아래 LBS 신고가 **사업자를 전제로 하는 절차인지** 확인 필요. ② Play 개인 계정은 **개발자 표시 이름이 실명으로 공개**될 수 있는데, 약관·처리방침은 운영주체를 "동아대학교 컴퓨터공학과 졸업작품팀 내친구지성", 책임자를 조성민으로 적고 있다 — **스토어 표기와 문서 표기가 어긋나면 심사에서 지적될 수 있다.** 둘을 맞출 것 |
| 스토어 등록정보 | 앱 설명·피처 그래픽(1024×500)·데이터 안전 양식·콘텐츠 등급 설문 |
| **스토어 스크린샷 — 재확보 필요** | 이전 감사에서 "`docs/presentation/screenshots/` 24장 재활용 가능" 으로 ✅ 처리했으나, **2026-07-29 문서 정리에서 `docs/presentation/` 이 제거되면서 사라졌다.** 이 경로는 `.gitignore:20` 대상이라 **git 이력에도 없다** — 저장소에서 복구할 수 없다. 캡처 스크립트(`capture_screens.mjs`·`recapture_screens.mjs`)는 추적 대상이었으므로 git 이력에서 되살릴 수 있다: `git show c2bdb39:docs/presentation/capture_screens.mjs`. 로컬 백업이 없다면 **재캡처가 필요하다** |
| **위치기반서비스(LBS) 신고** | 위치정보법상 위치기반서비스사업 신고 대상 여부 **확인 필요**. ⚠️ **사업자등록이 없다**(개인, 2026-07-29 확인) — 신고 절차가 사업자를 전제하는지, 비사업자 개인에게 어떤 경로가 있는지 함께 확인해야 한다. **법적 판단이므로 여기서 단정하지 않는다.** 다만 위치정보 이용약관 §1 이 「위치정보의 보호 및 이용 등에 관한 법률」을 근거로 명시하고 있어 **문서와 실제 신고 상태가 어긋날 여지**가 있다 |
| **위치정보 이용약관 페이지 부재** | 2026-07-29 실측: `/location-terms`·`/location`·`/terms/location` 모두 404 이고, 살아 있는 `/terms`·`/privacy` 본문에도 '위치정보' 문구가 **없다**. 그런데 **가입 화면은 이걸 필수 동의 3종으로 받고 있고**, 그 링크는 일반 `/terms` 를 가리킨다(`app/(auth)/signup.tsx:40`) — **동의받는 문서와 보여주는 문서가 다르다.** 프로필 메뉴에는 죽은 링크를 만들지 않으려 **노출하지 않았다**(상수 `LOCATION_TERMS_AVAILABLE` 한 줄로 켜짐). → 백엔드 backlog **§30-B** |
| 비밀번호 재설정 자동화 | `POST /auth/password/reset-request` 는 **스텁**(OpenAPI 설명: SMTP·토큰 저장 미구현, 항상 `{ok:true}`). 그래서 로그인 화면은 수동 안내를 유지한다 — 현재 문구가 사실과 맞다. → **§30-C** |
| ~~destinations 클라이언트 저장~~ | **✅ 해소** — backlog **§11 이 release 2026-06 에 배포**돼 `destinationStore` 가 **서버 소스 + AsyncStorage 오프라인 캐시**로 전환됐다(커밋 `ea9a33f`·`caf2d1f`). "기기 변경 시 외근 계획 유실" 은 §11 이전 상태였는데 이 표만 갱신되지 않고 남아 있었다 (2026-07-29 정정) |
| **의존성 취약점** | `npm audit` **20건** (critical 2 · high 7 · moderate 9 · low 2). GitHub dependabot 은 24건으로 집계. 출시 전 한 번 훑을 것 — 대부분 전이 의존성이라 `npm audit fix` 로 안 잡힐 가능성이 높고, Expo SDK 업그레이드 주기와 묶어 판단해야 한다 |
| 보안 정리 | `eas.json` 에 카카오 JS 키 커밋됨 (README 의 키 커밋 금지 규칙 위반). 도메인 제한 키라 치명도는 낮으나 EAS Secrets 이전 + 키 재발급 권장 |
| 백엔드 운영 내성 | 단일 서버 구성으로 추정 — 장애 시 앱 전면 불능. 출시 규모에선 허용 가능하나 인지 필요 |

## 권장 순서 (해소 시나리오)

1. ~~⛔-3·4 (프론트 설정 정리)~~ ✅ 완료 (2026-06-07, 타입체크 통과)
2. ~~⛔-2 (백엔드 정적 페이지)~~ ✅ 완료 (§23 배포) — 단 **본문은 초안**, 심사 전 교체
3. ~~⛔-1 (아이콘)~~ ✅ 완료 (2026-07-24 브랜드 아이콘 + 2026-07-28 adaptive 재가공)
4. ~~⛔-5 프론트 몫~~ ✅ 완료 (2026-07-29 `c577c31`) — 탈퇴 화면·API 계약·미구현 시 실패 처리
5. **다음 (백엔드)**: **§30-A `DELETE /api/me`** ← 지금 시점의 실질적 최우선. 이게 없으면 탈퇴 버튼이 실패 안내만 낸다
6. **그 다음 (팀/백엔드)**: §30-B 위치정보 이용약관 페이지 + `/terms`·`/privacy` 본문 초안 교체
7. **그 다음**: production 빌드(aab) 실기기 검증 → Play Console 등록정보·데이터 안전 양식
8. **마지막 — 줄일 수 없는 2주**: 비공개 테스트 **12명 × 14일** (개인 계정 확정). 이 시계는
   ⛔-5 가 닫혀야 시작된다

> **일정 관점 결론**: 나머지를 다 끝내도 마지막 2주는 남는다. 그리고 그 2주는 `DELETE /api/me`
> 가 나와야 시작된다 — **백엔드 §30-A 가 하루 늦으면 출시가 하루 늦는다.**

> **코드 쪽 차단은 프론트 기준으로 없다. 그러나 출시 차단은 남아 있다** — ⛔-5 는 백엔드
> `DELETE /api/me` 없이는 닫히지 않는다. 이전 판(2026-07-28)의 "코드 쪽 차단은 없다" 한 줄은
> 프론트 범위만 보고 전체를 단정한 표현이었다. 나머지 일정 요인은 「절차·품질」 표를 참조.

## 변경 이력

- **2026-07-29**: ⛔-5(앱 내 계정 삭제) 추가. `docs/REQUIREMENTS_BEFORE_LAUNCHING.md` 검토 중
  운영 OpenAPI 대조로 발견 — 사용자 삭제 엔드포인트가 스펙 전체에 0건이었다. 같은 검토에서
  위치정보 이용약관 페이지 부재와 비밀번호 재설정 스텁도 드러나 「절차·품질」에 추가했다.
  프론트 대응은 커밋 `c577c31`, 백엔드 요청은 backlog §30.
- **2026-07-28**: ⛔-1(아이콘) 해소로 초기 4건 종결.
